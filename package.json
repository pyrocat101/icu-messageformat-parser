{
  "name": "icu-messageformat-parser",
  "version": "0.1.0",
  "description": "MessageFormat parser that aims to be compatible with ICU4J syntax and intl-messageformat-parser output",
  "main": "dist/index.js",
  "module": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json && tsc -p tsconfig.build-cjs.json",
    "prepublish": "npm run build",
    "test": "jest",
    "clean": "rimraf lib"
  },
  "author": "",
  "license": "BSD-3-Clause",
  "devDependencies": {
    "@types/jest": "^24.0.15",
    "jest": "^24.8.0",
    "prettier": "^1.18.2",
    "rimraf": "^2.6.3",
    "ts-jest": "^24.0.2",
    "typescript": "^3.5.3"
  },
  "dependencies": {
    "@types/parsimmon": "^1.10.0",
    "parsimmon": "^1.12.1",
    "tslib": "^1.10.0"
  }
}
